# 使用手册

> Claude Code 多供应商切换工具（ccs）· 方案版本 v1.4
> 适用环境：Windows 10/11 · PowerShell 7 / PowerShell 5.1 / Git Bash
> 部署方法见 [部署指南.md](部署指南.md)

---

## 一、方案简介

本方案用于在多个大模型供应商之间管理和切换 Claude Code。核心是一个叫
**`ccs`** 的终端命令：敲一下弹出供应商菜单，输序号、名字或直接回车即以
对应供应商启动 Claude Code，退出后环境变量自动清理。

**能力一览：**

- ✅ 三个终端环境通用：PowerShell 7、PowerShell 5.1、Git Bash（行为完全一致）
- ✅ 菜单序号 / 名字直达 / 唯一前缀直达 / Tab 补全 / 直接回车 = 上次使用
- ✅ 不同终端窗口可**同时**跑不同供应商，互不干扰（多账号额度池并行）
- ✅ 会话内 `/model` 切模型，操作习惯与原版 Claude Code 完全一致
- ✅ 添加/删除供应商只需编辑一个 JSON 文件，无需改脚本
- ✅ `ccs list` 配置校验、`ccs status` 一屏排障（含重复 Key 检测）

## 二、文件清单与职责

| 文件 | 职责 | 需要日常维护？ |
|---|---|---|
| `~\.claude\suppliers.json` | **供应商注册表**（地址/密钥/模型） | ✅ 唯一需要维护的文件 |
| `~\.claude\suppliers.help.md` | JSON 格式说明文档 | 参考 |
| `~\.claude\claude-menu.ps1` | 菜单逻辑（PS5/PS7 共用，含 Tab 补全） | 否（升级时整体替换） |
| `~\.claude\claude-menu.sh` | 菜单逻辑（Git Bash 版，经 node 解析 JSON） | 否（升级时整体替换） |
| `~\.claude\.supplier-last` | 记录上次使用的供应商（自动生成，格式"名字<TAB>时间"） | 否 |
| 两个 PowerShell Profile、`~\.bashrc` | 各有一段条件加载块（部署脚本自动管理） | 否 |

## 三、日常使用

### 3.1 启动菜单

新开一个终端标签页，输入 `ccs`（PowerShell 里也可用全称 `Claude-Supplier`，
Git Bash 里是 `claude-supplier`）：

```
  ── Claude Code 供应商选择 ────────────────────

   [0]  official   官方 Anthropic  （未登录）
   [1]  glma       智谱GLM · 账号A  ● 上次
   [2]  glmb       智谱GLM · 账号B

  ──────────────────────────────────────────────
  回车 = 上次使用 · 序号或名字直达 · q 退出

  选择 [上次 1 glma] ▸ 
```

- 输入序号（`0`～`2`）或名字（`glma` 等）回车即启动，名字**不分大小写**
- **唯一前缀直达**：输入某几个字母，若恰好只匹配一家（如 `gla` → glma）直接
  启动；匹配多家则列出候选让你补全（如 `g` → glma/glmb）
- **直接回车** = 上次使用的供应商；提示符方括号里以弱化注释色实时显示回车
  将选中谁（如 `[上次 1 glma]`），无使用记录时不显示
- `official` 行尾自动标注登录状态：未登录显示 `（未登录）`，选中时也会提前
  提示将进入官方登录流程（登录一次后标注消失）
- `q` = 退出不启动（若存在名字就叫 `q` 的供应商，则优先选中它）
- 输错只提示一行错误后继续等待输入（**不重画菜单**），并给出
  "是不是想输 xxx?"的纠错建议，不会误启动

### 3.2 直达（跳过菜单）

```
ccs 1            # 按序号直达
ccs glma         # 按名字直达（不分大小写）
ccs gla          # 唯一前缀直达（只匹配 glma 一家时直接启动）
ccs 0            # 直达官方入口
```

### 3.3 参数透传

供应商后面的参数会原样转交给 claude：

```
ccs glma --continue       # 继续 glma 账号的上次会话
ccs 2 "帮我写个脚本"       # 启动时直接带提示词
ccs -p "..."              # 以 - 开头视为 claude 参数，仍弹菜单选供应商
```

### 3.4 会话内切换模型（/model）

进入会话后照常使用 `/model`。由于配置了映射，菜单里显示的是 Claude Code
的三档模型名，实际对应关系（以 GLM 为例）：

| /model 里的选项 | 实际调用的模型 | 说明 |
|---|---|---|
| Opus / Sonnet（大模型档） | `glm-5.3[1m]` | 主力档，两档无区别 |
| Haiku（轻量档） | `glm-5.3-flash[1m]` | 后台小任务走它，省积分 |

### 3.5 多终端并行

每个终端标签页是独立进程：标签 A 里 `ccs 1` 跑账号 A，标签 B 里 `ccs 2`
跑账号 B，互不影响——多个账号的额度池可以同时利用。

### 3.6 配置校验与状态查询

```
ccs list     # 或 ccs -l：无交互列出全部供应商 + JSON 格式校验
ccs status   # 或 ccs -s：上次使用 / 配置信息 / 官方登录 / Key 指纹与查重
```

`ccs status` 效果（Key 只显示头 6 位 + 尾 4 位指纹，不会泄露完整密钥；
两个供应商填了同一个 Key 时会显式警告——这是多账号配置最常见的事故）：

```
  ── ccs 状态 ─────────────────────────────────

  上次使用  : glma（智谱GLM · 账号A）· 2026-08-30 11:35
  供应商配置: C:\Users\<你>\.claude\suppliers.json（3 项，修改于 08-30 10:00）
  claude    : 2.1.251 (Claude Code)  [C:\...\npm\claude.cmd]
  官方登录  : ✘ 未登录（首次用 ccs 0 前，先手动跑一次 claude 完成登录）
  菜单      : v1.4 · PS7 ✔ / PS5.1 ✔ / Git Bash ✔

  供应商明细
    glma        a1b2c3…x9y8z  → open.bigmodel.cn
    glmb        9f8e7d…u6v5w  → open.bigmodel.cn
    official    OAuth 官方入口
  ✔ 无重复 Key
```

> `list` / `status`（含 `-l` / `-s` 写法）是保留子命令，不要用作供应商标识名。

### 3.7 补全技巧

敲 `ccs ` 后按 **Tab**，会列出所有供应商名字和 `list`/`status` 子命令
（从 JSON 动态读取，加供应商自动跟上）。PowerShell 下 Tab 是菜单式补全；
Git Bash 下输入行支持方向键 / Home / End 编辑（readline）。

## 四、供应商配置详解（suppliers.json）

### 4.1 字段约定

| 键 | 含义 |
|---|---|
| 顶层键名 | 供应商标识，命令行输入时用的名字（建议全小写字母数字） |
| `desc` | 菜单里显示的说明文字，随意写 |
| `env:XXX` | 启动 claude 前要设置的环境变量 XXX，可写多个 |
| `official` | 特殊条目：不带 `env:` 键，选中时清空相关环境变量直接启动官方 claude |

### 4.2 书写约束

1. 文件必须是**合法 JSON**。可以随意折行、加空格，值里也允许出现英文双引号
   ——三个终端统一按标准 JSON 解析（Git Bash 端通过 node 解析）
2. 键名建议只用小写字母/数字；`list` / `status` 是保留子命令，不要用作标识名
3. 菜单序号：`0` 固定是 official，其余按文件中出现顺序从 `1` 开始编号
   （供应商 ≥ 10 个时序号自动补齐位数，对齐不乱）
4. `desc` 建议不要包含制表符/换行（会被自动替换成空格）

修改 JSON 后**无需重启终端**，下一次运行 ccs 自动生效；改完可 `ccs list` 秒验。

### 4.3 各环境变量解释（GLM 示例）

| 变量 | 作用 |
|---|---|
| `ANTHROPIC_BASE_URL` | API 端点地址（必须是供应商的 **Anthropic 协议**端点） |
| `ANTHROPIC_AUTH_TOKEN` | 该账号的 API Key |
| `ANTHROPIC_DEFAULT_OPUS_MODEL` / `_SONNET_MODEL` | 大模型档映射 |
| `ANTHROPIC_DEFAULT_HAIKU_MODEL` | 轻量档映射（后台任务省积分） |
| `CLAUDE_CODE_AUTO_COMPACT_WINDOW` | 配合 1M 上下文的自动压缩阈值 |
| `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC` | 减少非必要流量（第三方端点建议开） |
| `API_TIMEOUT_MS` | 请求超时（长思考建议调大） |

### 4.4 常见操作

- **新增供应商**：照抄现有条目整块复制，改键名 / desc / token（标准 JSON 可折行）；
  换厂商（DeepSeek、Kimi 等）就换 BaseUrl 和 token，模型映射按各家文档调整——
  示例见 `suppliers.help.md`
- **删除供应商**：删掉对应条目。删掉 `official` 则菜单没有官方入口，序号从 1 开始
- **改默认模型 / 关闭 1M 上下文**：替换模型字符串；套餐不含百万上下文时报
  "模型不存在"，删掉 `[1m]` 后缀即可
- **清除"上次使用"记录**：删除 `~\.claude\.supplier-last`

## 五、工作原理（深入了解再看）

1. **环境变量是进程级的**：每个终端标签页是独立 shell 进程。菜单选中某供应商后，
   脚本在当前 shell 里设置一组 `ANTHROPIC_*` 环境变量再拉起 claude；claude 退出后
   脚本把这些变量全部删掉。这就是"不同终端并行不同供应商"和"退出自动还原"的原理。
   claude 被 Ctrl+C 打断时清理同样会执行（PowerShell 端用 try/finally 保证）。
2. **启动时锁定供应商**：环境变量是 claude 启动那一刻读取的，所以**切换供应商必须
   重新启动一次 claude**（`/exit` 后再敲一次 ccs），已经开着的会话切不了。
3. **数据与逻辑分离**：脚本只负责"读 JSON → 画菜单 → 设环境变量 → 启动"，
   不硬编码任何供应商信息。三个终端统一按标准 JSON 解析（Git Bash 端用 node）。
4. **清理范围动态生成**：official 入口启动前清理的变量 = 基础列表（`ANTHROPIC_BASE_URL`、
   `ANTHROPIC_AUTH_TOKEN`、`ANTHROPIC_API_KEY`、`ANTHROPIC_MODEL`、
   `ANTHROPIC_SMALL_FAST_MODEL`、三个 `ANTHROPIC_DEFAULT_*_MODEL`、
   `CLAUDE_CODE_AUTO_COMPACT_WINDOW`、`CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC`、
   `API_TIMEOUT_MS`）**∪ suppliers.json 里出现过的全部 `env:` 键**。因此给供应商新增
   `env:` 变量不需要同步改脚本。同一标签页先跑 A 再跑 B 时也会先全量清理再注入。
5. **模型映射**：Claude Code 内部按 Opus/Sonnet/Haiku 三档请求模型，
   `ANTHROPIC_DEFAULT_*_MODEL` 三个变量把这三档指向实际模型。Haiku 档承载了大量
   后台小任务（会话摘要、起标题等），映射到轻量模型可大幅降低消耗。
6. **启动前校验**：选中供应商后先确认 `claude` 命令存在，不存在时给出提示且不写入
   "上次使用"记录。
7. **编码约定**：`claude-menu.ps1` 为带 BOM 的 UTF-8（PS5.1 中文需要）；
   `claude-menu.sh` 为无 BOM UTF-8 + LF。

## 六、故障排查

| 症状 | 原因 | 解决办法 |
|---|---|---|
| 提示 `ccs` 不是命令 | 终端是部署**之前**开的；或 Profile 没挂载 | 新开标签页；`ccs status` 看三端挂载体检，缺哪端重跑 install.ps1 |
| 启动报模型不存在 | 套餐不含 1M 上下文 / 模型名写错 | 删掉 JSON 里的 `[1m]` 后缀或改模型名 |
| 切换供应商没生效 | 还在旧会话里 | `/exit` 退出后重新 ccs 启动 |
| 菜单里中文乱码（仅 PS5） | claude-menu.ps1 丢了 BOM | 用包内文件重新覆盖，或以"UTF-8 with BOM"重存 |
| bash 报 suppliers.json 解析失败 | JSON 不合法（少逗号、引号不闭合等） | 错误信息带具体行列号，按提示修复；`ccs list` 可随时校验 |
| bash 提示需要 node | node 不在 PATH | 检查 node 安装（claude-code 依赖 node） |
| official 启动却连不上官方 | 未登录过官方账号 | 终端里先手动跑一次 `claude` 完成登录（菜单会标注"未登录"） |
| 提示找不到 claude 命令 | npm 全局包异常 | `npm install -g @anthropic-ai/claude-code` 重装 |
| 担心 Key 填错/填重 | — | `ccs status` 看指纹与重复检测 |

## 七、安全注意事项

- `suppliers.json` 内是**明文 API Key**：不要提交到 git 仓库、不要截图或粘贴到
  公开场合、不要放进网盘同步目录
- 分发本方案文件夹时，**先删掉你自己机器上 `~\.claude\suppliers.json` 的内容**
  ——包内的 `suppliers.template.json` 只是占位模板，正常不含密钥，但别把真实
  配置一起拷给别人
- `ccs status` 的 Key 指纹（头 6 + 尾 4 位）仅用于本机核对

## 八、卸载

见 [部署指南.md](部署指南.md) 第六节（`uninstall.ps1`，默认保留 suppliers.json）。

## 九、版本历史

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 2026-08-30 | 初版：三终端供应商菜单、多账号 + 官方入口、自动化验证 |
| v1.1 | 2026-08-30 | 清理列表动态化（新增 env: 变量零脚本改动）；PS 端 try/finally；bash 端 node 解析标准 JSON；启动前校验 claude 命令；名字大小写不敏感；切换时全量清理 |
| v1.2 | 2026-08-30 | 输错不重画菜单 + 纠错建议；提示符显示上次供应商；新增 ccs list / ccs status；统一宽度标题线；序号位数补齐；readline 编辑；名字优先于 q 退出键 |
| v1.3 | 2026-08-30 | official 登录状态标注；唯一前缀直达；status 全面增强（Key 指纹+查重、官方登录、上次时间、明细表、三端挂载体检、版本号）；列宽自适应；动态提示行；补全纳入子命令 |
| v1.3.1 | 2026-08-30 | 三端行为对齐收尾（bash 子命令大小写、PS 直达建议）；死代码清理；标准化分发包（install/uninstall/文档） |
| v1.4 | 2026-08-30 | 新增远程一键安装：`install-remote.ps1` 引导器从 GitHub 仓库下载部署（`irm <url> \| iex` 一句完成），含 jsDelivr 镜像自动回退（国内网络友好）；本地 `install.ps1` 增加 `-SrcDir` 参数支持远程调用；远程卸载 `irm <url>/uninstall.ps1 \| iex` |
