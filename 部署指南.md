# 部署指南

> 目标：10 分钟内在你的 Windows 上获得 `ccs` 命令（Claude Code 多供应商切换菜单），
> 三终端（PowerShell 7 / PowerShell 5.1 / Git Bash）行为一致。

---

## 一、前置条件检查

| 项目 | 检查方法 | 不满足时 |
|---|---|---|
| Windows 10/11 | — | 本方案仅适配 Windows |
| Node.js | 终端运行 `node --version` | 到 [nodejs.org](https://nodejs.org) 安装 LTS |
| Claude Code | 终端运行 `claude --version` | `npm install -g @anthropic-ai/claude-code` |
| PowerShell | 系统自带 5.1 即可；PS7 可选 | — |
| Git Bash | 可选（不装则少一个终端入口） | 到 [git-scm.com](https://git-scm.com) 安装 |

> install.ps1 也会自动做以上检查并给出提示，但不阻断部署。

## 二、一键部署（推荐）

1. 把整个 `Claude_Cli_Multi_Supplier_Switch` 文件夹放到任意位置（如 `D:\tools\`）
2. 在该文件夹打开 PowerShell（地址栏输入 `powershell` 回车，或右键"在终端中打开"）
3. 运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1
```

脚本会自动完成（每一步都有输出，重复运行安全幂等）：

- ✅ 环境检查（claude / node，仅提示不阻断）
- ✅ 复制 `claude-menu.ps1`、`claude-menu.sh`、`suppliers.help.md` 到 `~\.claude\`
- ✅ 首次部署时从模板创建 `~\.claude\suppliers.json`（已存在则**保留不动**）
- ✅ 向 PowerShell 7 / PowerShell 5.1 的 Profile 和 `~\.bashrc` 末尾追加菜单挂载块
  （已挂载则跳过；追加前自动生成 `.bak-时间戳` 备份；不装 Git Bash 可加 `-SkipBash` 参数）

4. 部署完成后做两件事：

```text
① 编辑 %USERPROFILE%\.claude\suppliers.json，把两处"在此填入账号X的APIKey"
   占位符替换为真实 API Key（供应商增删改也在此文件，见 使用手册.md）
② 想用官方入口（ccs 0）的话：先在终端直接运行一次 claude 完成官方账号登录
```

## 三、手动部署（备选）

如果不想运行脚本，按以下步骤操作，效果完全相同。

**第 1 步：复制菜单文件**

把以下 4 个文件复制到 `C:\Users\<你的用户名>\.claude\`（目录不存在就创建）：

```text
claude-menu.ps1        →  ~\.claude\claude-menu.ps1
claude-menu.sh         →  ~\.claude\claude-menu.sh
suppliers.help.md      →  ~\.claude\suppliers.help.md
suppliers.template.json →  ~\.claude\suppliers.json（改名为 suppliers.json）
```

**第 2 步：挂载 PowerShell 7 Profile**

编辑 `文档\PowerShell\Microsoft.PowerShell_profile.ps1`（不存在就创建，
注意 PS7 在 `PowerShell` 目录、PS5.1 在 `WindowsPowerShell` 目录），
在文件**末尾**追加：

```powershell
# ---------- Claude Code 供应商菜单 (ccs / Claude-Supplier) ----------
if (Test-Path "$HOME\.claude\claude-menu.ps1") { . "$HOME\.claude\claude-menu.ps1" }
```

**第 3 步：挂载 PowerShell 5.1 Profile**

编辑 `文档\WindowsPowerShell\Microsoft.PowerShell_profile.ps1`，末尾追加同上两行。

**第 4 步：挂载 Git Bash（可选）**

编辑 `C:\Users\<你的用户名>\.bashrc`（不存在就创建；若存在 `.bash_profile`
且其中没有加载 `.bashrc` 的语句，需补上 `test -f ~/.bashrc && . ~/.bashrc`），
在文件**末尾**追加（注意此文件必须保存为**无 BOM 的 UTF-8、LF 换行**）：

```bash
# ---------- Claude Code 供应商菜单 (ccs / claude-supplier) ----------
if [ -f "$HOME/.claude/claude-menu.sh" ]; then
    . "$HOME/.claude/claude-menu.sh"
fi
```

**注意事项（手动部署容易踩的坑）**

- `claude-menu.ps1` 必须保持**带 BOM 的 UTF-8** 编码，否则 PS5.1 下中文乱码
- `.bashrc` 的追加块必须 LF 换行，CRLF 会导致 bash 报"找不到文件"
- 手动编辑 Profile 前先备份一份

（以上坑一键部署脚本都已处理，这也是推荐用它原因。）

## 四、部署后验证

新开一个终端标签页（Profile 只在 shell 启动时加载，老标签页看不到新命令）：

```text
ccs list      → 应列出模板里的 3 个条目并显示"✔ suppliers.json 正常"
ccs status    → 应显示配置路径、claude 版本、三端挂载 ✔、（未填 Key 时的）指纹占位
ccs           → 应弹出选择菜单，输入 1 回车启动（填好 Key 后即可正常对话）
```

三个终端（PS7 / PS5.1 / Git Bash）各开一个标签页重复上面的验证。
Git Bash 下按 Tab 应能补全供应商名。

## 五、升级到新版本

本方案的升级 = 覆盖两个菜单脚本：

1. 用新版 `claude-menu.ps1` / `claude-menu.sh` 覆盖 `~\.claude\` 下的同名文件
2. 直接重跑 `install.ps1` 也行（幂等：suppliers.json 保留、已挂载的 Profile 跳过）

`suppliers.json` 与 Profile 挂载块保持不变，无需重新配置。

## 六、卸载

```powershell
powershell -ExecutionPolicy Bypass -File .\uninstall.ps1
```

- 自动从三个 shell 配置里移除挂载块（修改前自动备份 `.bak-时间戳`）
- 删除 `~\.claude\` 下的菜单脚本三件套
- **suppliers.json（含 API Key）默认保留**；想彻底清除请加 `-Purge` 参数
- Claude Code 本体不受影响
- 通过远程方式安装的用户，卸载同样只需一句：
  `irm https://raw.githubusercontent.com/ldtmore/Win_Claude_Cli_Supplier_Switch/main/uninstall.ps1 | iex`

## 七、远程一键安装（一句命令）

本仓库已支持远程安装，分发对象**无需下载任何文件**，在 PowerShell 执行一句即可部署：

```powershell
irm https://raw.githubusercontent.com/ldtmore/Win_Claude_Cli_Supplier_Switch/main/install-remote.ps1 | iex
```

- `install-remote.ps1` 会自动下载全部文件、以脚本块方式执行安装器
  （不受执行策略限制），效果与本地安装完全一致（幂等、可重复执行）
- 大陆网络访问 GitHub raw 失败时自动回退 jsDelivr 镜像，也可直接用镜像地址：
  `irm https://cdn.jsdelivr.net/gh/ldtmore/Win_Claude_Cli_Supplier_Switch@main/install-remote.ps1 | iex`

## 八、仓库维护（如何发布升级）

本仓库的工作目录即 `Claude_Cli_Multi_Supplier_Switch` 文件夹，改完文件后：

```bash
git add .
git commit -m "v1.x: 改动说明"
git push
```

用户重跑同一条远程安装命令即可拿到新版（幂等，他们的 suppliers.json 不受影响）。

**注意事项：**

- `.gitignore` 已排除 `suppliers.json`（真实 Key）与 `*.bak-*` 备份——
  **永远不要**把真实配置放进这个文件夹再提交
- jsDelivr 镜像有约 12~24 小时缓存延迟，紧急更新可提醒用户优先用 GitHub raw 地址
- 发布新版本时同步更新：两个菜单脚本的版本常量（`ClaudeMenuVersion` /
  `_CLAUDE_MENU_VERSION`）、README 徽章与页脚、使用手册版本历史

